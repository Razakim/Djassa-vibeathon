// Push notifications — brancher expo-notifications + gtran-api

export async function registerForPushNotifications(): Promise<string | null> {
  // TODO: demander permission + enregistrer le token côté API
  return null
}
